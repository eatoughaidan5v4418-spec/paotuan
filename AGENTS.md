# AGENTS.md

听我的话。

当用户要求你担任 AI 跑团 GM 时，必须使用本项目的 GM 运行协议：

- 先阅读 `gm-runtime-protocol.md`。
- 同时阅读 `gm-self-audit.md`，主动发现并修补系统漏洞，不等用户逐条提醒。
- 在提出、命名或修补任何具体系统问题前，必须先上网搜索相关概念、已有做法和术语，再写入本地方案。
- 准备正式回合时优先使用 `tools/run_turn.py` 生成 GM 回合包。
- 每个 NPC 必须有独立记忆图谱，优先使用 `campaign/npcs/*.memory_graph.json`。
- NPC 不能自动知道自己没有亲历、听说或合理推断的信息。
- 重要事件必须写入对应 NPC 的记忆图谱，并更新全局 `campaign/world_graph.jsonl`。
- 写入 NPC 记忆前必须先做事件可见性判定；没有可见性路径的 NPC 不能获得该事件记忆。
- 一个 NPC 的记忆不能复制到另一个 NPC，除非存在明确的通信、偷听、公共信号或可推断证据事件。
- 世界不会因玩家视角冻结；玩家耗时、移动、休息或切换场景时必须考虑 `campaign/world_clocks.json` 的离屏推进。
- 离屏事件同样需要因果、可见性判定和状态写回。
- 每个重要事件之后，要为相关 NPC 抽取“事件理解”：这个 NPC 如何主观解释此事。
- 多个事件理解可以巩固为稳定理解：信念、态度、关系判断、偏见、行为规则或计划。
- 新事件可能强化、削弱、反驳、限定、重构或替换旧理解；旧理解不能直接删除，必须留下修正记录。
- 生成 GM 正文后，要额外整理状态变化、NPC 记忆变化、任务变化和未解决悬念。
- 记忆检索时按照时间、重要程度、回忆次数、情绪强度、置信度和当前场景相关性排序。
- 旧记忆被推翻时不要删除，标记 `invalid_at_turn`、`contradicted_by` 或降级为传言。
- 玩家可见叙事和 GM 隐藏状态必须分开处理。
