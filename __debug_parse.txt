def parse_zones(text: str) -> tuple[list[dict[str, Any]], list[dict[str, Any]], list[str]]:
    """Extract zones and connections from location YAML text. Returns (zones, connections, issues)."""
    zones: list[dict[str, Any]] = []
    connections: list[dict[str, Any]] = []
    issues: list[str] = []

    # Extract zones section using indentation
    zones_match = re.search(r'\nzones:\n((?:  .+\n?)*)', text)
    if zones_match:
        zone_block = zones_match.group(1)
        # Split into indiv